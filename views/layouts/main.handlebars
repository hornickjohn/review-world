<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/style.css">
    <title>Review World</title>
</head>
<body>
    <header>
        <h1>Review World</h1>
         <nav class="navbar bg-body-tertiary">
            {{#if currentUserData.loggedIn}}
            <div class="container-fluid">
            <a class="navbar-brand" href="/logout">Log out</a>
            <a class="navbar-brand" href="/profile">Profile</a>
            <a class="navbar-brand" href="/addreview">New review</a>
            <a class="navbar-brand" href="/account">Update account</a>
            {{else}}
            <a class="navbar-brand" href="/login">Log in</a>
            <a class="navbar-brand" href="/signup">Sign up</a>
            </div>
            {{/if}}
            <a class="navbar-brand" href="/">Home</a>
            <form class="navbar-brand" action="/search">
                <select name="category" id="cars">
                    <option value="any">--Any--</option>
                    {{#each categoryData}}
                    <option value="{{this.name}}">{{this.name}}</option>
                    {{/each}}
                </select>
                <input name="term" type="text" placeholder="search products">
                <label for="minstars">Min Star Rating</label><input id="minstars" name="min" type="number" max="5" min="1" placeholder="1-5">
                <label for="maxstars">Max Star Rating</label><input id="maxstars" name="max" type="number" max="5" min="1" placeholder="1-5">
                <button>Search</button>
            </form>
        </nav>
    </header>
    <main>
            {{{body}}}
    </main>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js" integrity="sha384-w76AqPfDkMBDXo30jS1Sgez6pr3x5MlQ1ZAGC+nuZB+EYdgRZgiwxhTBTkF7CXvN" crossorigin="anonymous"></script>
</body>
</html>